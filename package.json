{
  "name": "moderation-shift-bot",
  "version": "1.0.0",
  "description": "Discord bot for moderation shift reminders and schedule sync.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/bot/createBot.js && node --check src/services/reminderService.js && node --check src/store/shiftStore.js && node --check src/web/createServer.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "discord.js": "^14.19.3",
    "dotenv": "^16.4.5",
    "express": "^4.21.2"
  }
}
